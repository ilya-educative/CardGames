package com.blocker.cardgames.matchmaking;

public class MatchmakingSystem {

    public static void main(String[] args) {

        // MatchmakingSystem<> matchmaking = new MatchmakingSystem(new InMemoryQueue());
        // matchmaking.join(session, new GameConfiguration(GameType.Classic, ...));
        //  [when Client joins: he must be either added to existing queue, or to newly created queue for this GameType]
        // matchmaking.leave(session);
        //  [when Client leaves: he must be removed from the queue]
        // matchmaking.confirm(session);
        //  [client confirms that he is ready to start]
        // session - it is a client identifier
        //
    }
}
